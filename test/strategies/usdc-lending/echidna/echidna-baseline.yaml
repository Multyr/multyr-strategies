testMode: property  # echidna_* functions return bool (true = invariant holds)
testLimit: 1000000
seqLen: 200
shrinkLimit: 20000
corpusDir: "test/strategies/usdc-lending/echidna/corpus"
coverage: true
format: "text"
workers: 8
